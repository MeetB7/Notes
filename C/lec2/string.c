// String is an array of characters.
#include <stdio.h>
#include <cs50.h>

int main (void)
{
    string s = "HI!";
    printf("First char is %i \n", s[3]);
    // the NULL (0 ascii) (respresented by \0 ) marks the end of a string or array of characters

}
// you can treat chars as numbers